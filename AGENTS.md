# AGENTS.md

本文件是给 AI 智能体（Claude Code 等）的工作要求。

## 通用原则：最小改动（重要）

只做完成当前任务所必需的**最小改动**：不顺手重构、清理或引入无关变更；能用现有机制解决就不新增文件、规则或依赖；需要扩大改动范围前先确认。

## CSS 约定

- 站点个性化样式**一律追加到主题预留的 `themes/hugo-prose/static/css/custom.css`**（上游该文件为空，专门留给站点定制）。
- **禁止**新建 `static/css/site.css` 之类的独立样式文件，也**不要**把个性化样式写进主题的 `style.css` / `fonts.css` 等文件。
- **不引入不必要的 CSS**：能用现有样式或 CSS 变量解决的，不要新增规则；确需新增时保持最小改动，并集中追加到 `custom.css`。
